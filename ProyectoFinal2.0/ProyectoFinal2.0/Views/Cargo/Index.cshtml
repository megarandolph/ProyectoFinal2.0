@model IEnumerable<ProyectoFinal2._0.Cargo>

@{
    ViewBag.Title = "Index";
}

<h2>inicio</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cargo1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cargo1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.cargoId }) |
            @Html.ActionLink("Detalle", "Detalles", new { id=item.cargoId }) |
            @Html.ActionLink("borrar", "Borrar", new { id=item.cargoId })
        </td>
    </tr>
}

</table>
