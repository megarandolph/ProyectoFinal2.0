@model IEnumerable<ProyectoFinal2._0.Salidas>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.motivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_salida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empleados.emp_codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_salida1.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.motivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_salida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empleados.emp_codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_salida1.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Editar", new { id=item.salidaId }) |
            @Html.ActionLink("Details", "Detalles", new { id=item.salidaId }) |
            @Html.ActionLink("Delete", "Borrar", new { id=item.salidaId })
        </td>
    </tr>
}

</table>
